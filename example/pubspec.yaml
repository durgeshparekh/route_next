name: route_next_example
description: Example application for route_next package.
publish_to: 'none'
version: 1.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  route_next:
    path: ../

flutter:
  uses-material-design: true
